# tests/e2e

**Responsable:** Tomás Rosales (testing).

Scripts de pruebas end-to-end con Selenium sobre `apps/web`.

Vacío por ahora. Cuando se agreguen tests, apuntar los selectores a los atributos `data-testid`
documentados en el `CLAUDE.md` raíz (sección "Acciones interactivas clave") en vez de clases CSS o
texto visible, para que no se rompan con cambios de estilo o de copy.
